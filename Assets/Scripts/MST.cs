using System.Collections.Generic;

public static class MST
{
    public static List<Edge> GetMST(List<Edge> edges)
    {
        var o = new List<Edge>();
        var current = new List<Edge>(edges);

        while (current.Count != 0)
        {
            
        }
        
        
        return o;
    }
}